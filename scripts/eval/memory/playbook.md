# Playbook (hand-written memory bullets, the run-time-loop treatment)

Prepended to the task prompt in the `memory` condition. Replace with bullets
relevant to YOUR tasks — the A/B only has signal when a bullet addresses a
gotcha the baseline agent actually trips on. The `add` task is a plumbing smoke
(trivial → both conditions pass → no signal); it's here to prove the rig runs.

- When editing a file to satisfy a test, run the test mentally on edge cases
  (negatives, zero) before declaring done.
