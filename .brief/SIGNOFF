# brief backfill — conformance sign-off
vault-egress-default-deny conforms: Host-owned OAuth refresh removes response-leg token rotation only; host classification and the libkrun default-deny DNS/IP fence are unchanged, with no new pass-through or pooling path.
managed-tier-do-gateway conforms: Approved external cost receipts bind observed commit and lifecycle usage to immutable runtime evidence; no runtime persistence, custom DO, collaboration authority, or commit-order change is introduced.
optimization-external-substrate-primitives conforms: Foreground producer-health and token-overflow checks observe the existing session event stream; no optimizer, routing policy, or reward authority is embedded.
substrate-plane-livesession conforms: Guard health remains local event-producer monitoring through existing session resolution; no backend-string dispatch, capability change, or new backend is added.
# The backfill commit adds `// Context:` doc-refs (comments only) into governed
# source sites and reconstructs the decision layer. No pillbox behavior changed,
# so every touched decision still conforms by construction.

libkrun-is-the-backend conforms: backfill adds a doc-ref comment only; select_backend still defaults to libkrun (mod.rs) and default = ["libkrun"] is unchanged.
docker-backend-deleted conforms: no code change to the docker backend; only a doc-ref/comment was added to sandbox/mod.rs.
qemu-parked conforms: no QEMU backend added; sandbox/mod.rs change is a doc-ref comment only.
vault-broker-oauth conforms: refresh.rs change is a doc-ref comment above pre_refresh; broker/pre_refresh logic unchanged.
docs-governance conforms: AGENTS.md gained only the brief convention block (via init); the router/canonical-doc discipline is unchanged.
rustic-cache-variant-gated conforms: rustic.rs change is a doc-ref comment above repo_opts; per-variant cache logic unchanged.
dx-zero-config-local conforms: events/log.rs change is a doc-ref comment; the zero-config local subscribe surface is unchanged.
libkrun-env-fork-substrate conforms: libkrun/session.rs change is a doc-ref comment at the env-fork invariant; the env-fork channel split is unchanged.
managed-tier-do-gateway conforms: sandbox/mod.rs change is a doc-ref comment only; the managed placement behind the trait is unchanged.
remote-backend-plane-removed conforms: no remote backend re-added; sandbox/mod.rs change is a doc-ref comment only.
secret-on-disk-0600 conforms: paths.rs/rustic.rs changes are doc-ref comments; the 0600 write helper and its callers are unchanged.
session-event-log-spine conforms: events/log.rs change is a doc-ref comment above append; the single-writer per-session seq logic is unchanged.
stable-exit-codes conforms: errors.rs change is a doc-ref comment; the ExitCategory numeric contract is unchanged.
substrate-plane-livesession conforms: sandbox/mod.rs change is a doc-ref comment above the LiveSession trait; the trait/Caps surface is unchanged.
workspace-ingest-sovereignty conforms: ingest.rs/libkrun session.rs changes are doc-ref comments; the pillbox-controlled secret denylist is unchanged.
libkrun-is-the-backend conforms: workspace-fork source selection inside the libkrun backend; adds no backend and does not touch backend selection.
libkrun-env-fork-substrate conforms: forks the env-fork substrate's workspace from a warm base cache via CoW (see doc://pillbox/workspace-cow-fork); substrate invariant unchanged.
workspace-ingest-sovereignty conforms: edits launch_base clone-source selection in session.rs; the ingest denylist / sovereignty invariant is untouched.
managed-tier-do-gateway conforms: the Cloudflare workspace-transfer path now forwards the prefix-scoped R2 credential's required session token into the container helper, satisfying the frozen fail-closed credential contract.
managed-store-of-record conforms: scoped credential propagation changes only authentication for rustic-over-R2 provision/finalize; rustic remains the authoritative cross-placement store and handle space.
libkrun-is-the-backend conforms: Cargo metadata now describes the public Cloudflare/local positioning; the default libkrun feature and backend selection are unchanged.
docs-governance conforms: the docs router and canonical subsystem docs were updated to match shipped code and active decisions, reducing rather than introducing drift.
secret-on-disk-0600 conforms: the workspace helper reads an optional R2 session token from its process environment and changes no private-file write path; the repo password still uses write_private_file.
managed-tier-do-gateway conforms: records the live scoped-R2 foreground proof and updates the deployment guide; the per-session Durable Object remains the sequencer, actor authority, driver arbiter, replay store, and container gateway required by the decision.
libkrun-env-fork-substrate conforms: the guest boot preamble now runs update-ca-certificates via a PATH including /usr/sbin so the vault MITM CA cert lands in the guest trust store; no real credential enters the guest env/argv/VmSpec and the host-owned smoltcp/MITM termination point is unchanged.
workspace-ingest-sovereignty conforms: change is confined to the CA-install line of the boot preamble; the pillbox-controlled ingest denylist and the workspace virtio-fs mount are untouched.
libkrun-is-the-backend conforms: the change is a bugfix inside the libkrun backend's guest boot preamble (CA install); it adds no backend, does not touch `default = ["libkrun"]`, and does not alter backend selection.
managed-store-of-record conforms: the per-session ensure binding adds only Durable Object coordination metadata; it does not store workspace contents or alter rustic-over-R2 as the authoritative managed workspace store and handle space.
managed-tier-do-gateway conforms: private ensure-session RPC keeps atomic session identity state in the per-session Durable Object; the DO remains the server-side gateway and no host provisioning or public ensure route is added.
managed-tier-do-gateway conforms: private Huddles invocation claims one exact rendered delivery before the existing DO-to-container turn, records raw input/output in the per-session log, and adds no host provisioning, public route, or orchestration semantics.
managed-store-of-record conforms: the invocation row and §0 events are session execution metadata only; managed workspace contents remain recoverable from the unchanged rustic-over-R2 store and handle space.
managed-tier-do-gateway conforms: an ensured Huddles SessionGateway now fails closed on public HTTP and WebSocket attach/drive, including sockets connected before binding; private service-binding ensure/invoke and the per-session seq authority remain unchanged.
managed-store-of-record conforms: redacted invocation diagnostics add only session execution evidence to the existing Durable Object log; workspace contents and the authoritative rustic-over-R2 handle space are unchanged.
managed-tier-do-gateway conforms: the per-session Durable Object still owns sequencing and the private container invocation; this change retains a redacted caught-exception cause in its own log while the Huddles RPC failure envelope remains generic.
managed-tier-do-gateway conforms: the SessionGateway deterministically projects session identity into Cloudflare's 63-character Sandbox namespace at the DO-to-container adapter boundary; the per-session Durable Object still owns sequencing, attribution, replay, and container lifecycle.
managed-store-of-record conforms: sandbox runtime identity projection changes no SessionRef, durable event identity, workspace content, or rustic-over-R2 handle; it only supplies a valid Cloudflare container address.
managed-tier-do-gateway conforms: private Huddles invocation now carries a generic JSON-schema output constraint into OpenCode and projects the resulting structured value through the existing per-session event stream; the Durable Object remains the sequencer, gateway, and container lifecycle authority.
managed-store-of-record conforms: schema-bound output is session execution evidence only; it changes no workspace content authority or rustic-over-R2 handle.
session-event-log-spine conforms: OpenCode structured output becomes an ordinary MessageDelta in the existing immutable, single-writer session log without changing the §0 event schema or sequence authority.
agent-io-pty-free-contract conforms: the OpenCode mapper projects schema-bound output from the existing structured SSE API into session evidence; it adds no PTY, terminal scraping, or alternate agent I/O path.
managed-store-of-record conforms: bounded structured-output retries, normalized terminal output, and their positional session evidence remain execution metadata in the per-session log; workspace contents and the authoritative rustic-over-R2 handle space are unchanged.
managed-tier-do-gateway conforms: the extracted OpenCode turn driver remains inside the private per-session SessionGateway boundary; the Durable Object still owns sequencing, actor attribution, replay, invocation identity, and container lifecycle, with no host provisioning or public route added.
session-event-log-spine conforms: requested profiles remain immutable session/run evidence, while served-model and effective-limit facts are reduced only from the same session's append-only log with the enclosing event seq as source position.
agent-io-pty-free-contract conforms: the shared structured contract now transports provider/model/profile/reasoning requests and observed runtime evidence without adding threads, task policy, locks, or a PTY integration.
agent-io-pty-free-contract conforms: Cursor Agent CLI stream-json adapter normalizes headless `agent -p` output into the shared contract vocabulary without adding PTY, threads, or orchestration policy.
agent-io-pty-free-contract conforms: pi moves to Integration::Structured with the shared one-shot drain; no PTY path added.
dx-zero-config-local conforms: session info JSON now surfaces requested vs served model evidence from the existing local session log; no daemon or remote config required.
gateway-no-daemon conforms: session info evidence reduction stays in-process against the local §0 log; no new gateway or long-lived daemon.
libkrun-env-fork-substrate conforms: cursor alt-auth and structured one-shot launch still use launch_base CoW forks; no real credential enters guest argv/VmSpec beyond the existing --with/env path; egress allowlist gains only Cursor hosts.
optimization-external-substrate-primitives conforms: RequestedRunProfile/served evidence are session evidence fields only; no optimizer/policy logic enters the substrate.
substrate-plane-livesession conforms: session info JSON evidence is a read of the existing LiveSession log; send/attach/watch surfaces are unchanged.
vault-egress-default-deny conforms: Cursor Agent CLI hosts are an explicit allowlist addition; default-deny and the DNS fence remain.
workspace-ingest-sovereignty conforms: structured/cursor launch uses the existing workspace virtio-fs mount; no ingest denylist change.
libkrun-is-the-backend conforms: Cursor/pi Structured runs are libkrun-only; docker rejects them rather than gaining a parallel path.
docker-backend-deleted conforms: docker rejects Integration::Structured rather than growing a docker structured path; no new docker features.
runner-image-tags-name-roles conforms: build-runner.sh only adds the agent binary to the PATH check list; image tag roles are unchanged.
runner-image-tags-name-roles conforms: the updater now refreshes every bundled harness while retaining the dev/latest/vX.Y.Z tag roles.
qemu-parked conforms: sandbox/mod.rs only adds the structured module gate; QEMU remains parked and unselected.
managed-store-of-record conforms: managed Session records gain an optional requested_execution field with default None; rustic-over-R2 workspace authority is unchanged.
managed-tier-do-gateway conforms: managed.rs only fills the new Session field as None; DO gateway sequencing and drive path are untouched.
remote-backend-plane-removed conforms: structured module is local libkrun-only; no remote URL backend returns.
libkrun-is-the-backend conforms: rustfmt-only whitespace in libkrun session structured path; backend selection unchanged.
agent-io-pty-free-contract conforms: rustfmt-only whitespace in cursor harness; adapter vocabulary unchanged.
libkrun-env-fork-substrate conforms: rustfmt-only whitespace in structured launch; CoW fork/env substrate unchanged.
workspace-ingest-sovereignty conforms: rustfmt-only whitespace in session.rs; ingest denylist untouched.
managed-tier-do-gateway conforms: v0002 removes the custom SessionGateway and remote §0 placement; Huddles owns collaboration while Pillbox persists bounded D1 claims, one immutable R2 evidence object, one terminal analytics point, and uses only Cloudflare Sandbox's vendor-owned DO.
managed-tier-do-gateway conforms: signed execution grants are verified and re-introspected statelessly before bounded D1 execution; no SessionGateway, CredentialBroker, or other Pillbox-authored Durable Object is bound or exported.
managed-store-of-record conforms: signed grants remain authorization input only; execution identity stays in bounded D1 and evidence in immutable R2, with no OAuth or token material persisted.
libkrun-is-the-backend conforms: structured-run payload bounds, cost evidence, and workspace lineage checks stay inside the selected libkrun backend; backend selection and the default feature are unchanged.
docker-backend-deleted conforms: docker only rejects the structured integration introduced for libkrun; it gains no feature, parity promise, or new engineering path.
qemu-parked conforms: sandbox module wiring exposes structured execution without selecting or implementing a QEMU backend; QEMU remains parked.
docs-governance conforms: AGENTS.md and the canonical docs record the ratified single-controller managed boundary, Durable Object default-deny policy, and storage/cost contracts without creating a competing source of truth.
agent-io-pty-free-contract conforms: requested model profiles, served-model evidence, usage, and bounded structured output travel through the existing normalized event contract; no PTY or terminal-scraping path is added.
dx-zero-config-local conforms: local session status and cost evidence continue to derive in-process from the local session log; no daemon, managed gateway, or remote configuration is required.
gateway-no-daemon conforms: the remote event source/sink and custom managed gateway are removed; the remaining local gateway stays in-process and introduces no long-lived daemon.
libkrun-env-fork-substrate conforms: structured launches and their payload limits retain the existing launch-base CoW fork and environment channel; no real credential is added to guest argv or VmSpec.
optimization-external-substrate-primitives conforms: model, usage, and cost fields are sourced execution evidence only; no optimizer, routing policy, or learning loop enters the substrate.
remote-backend-plane-removed conforms: managed Cloudflare execution remains a fresh single-controller runtime reached through its explicit managed placement; no remote URL backend or legacy remote control plane returns.
secret-on-disk-0600 conforms: scoped workspace credentials remain process-bound and the rustic repository password still uses the private-file path; no secret-bearing file permission is weakened.
session-event-log-spine conforms: the local append-only SessionLog remains the sole per-session event spine with monotonic sequence assignment; remote source/sink routing and DO log placement are removed.
substrate-plane-livesession conforms: structured execution, status evidence, and cost projection preserve the existing LiveSession capability boundary; send, attach, and watch ownership do not move into orchestration.
workspace-ingest-sovereignty conforms: managed lineage verification and structured local launches keep the existing workspace mount and ingest denylist; no untrusted workspace file becomes credential or policy authority.
managed-tier-do-gateway conforms: PB-005 atomically reserves a bounded singleton D1 allowance only for new invocation claims, fails closed before Sandbox/R2/Analytics when disabled or exhausted, and introduces no custom Durable Object, event rows, scans, timers, or automatic reset.
vault-broker-oauth conforms: PB-006 moves OAuth token shape, expiry, refresh request/application, and placement-specific stubs behind the provider registry while preserving single-writer pre-refresh, far-future guest expiry, and access-only Codex release inside libkrun.
vault-broker-oauth conforms: PB-019 deletes guest-driven PendingFlow/response mutation and teardown persistence, rejects Anthropic/Codex token endpoints locally, withholds libkrun refresh-token release pairs, and keeps pre-run/JIT rotation behind OAuthCodec + TokenStore.
managed-tier-do-gateway conforms: PB-008 verifies a separate operation-scoped Huddles grant before generic execute/status/cancel persistence or charged access, rechecks currentness on exact retries, and returns no authorization identity to bounded D1, immutable R2 evidence, Sandbox runtime state, or Analytics.
managed-tier-do-gateway conforms: PB-009 retires the historical managed ensure/invoke grant surface after the connected v2 cutover; production Huddles RPC now exposes only authorization-gated execute/status/cancel, while local legacy compatibility is isolated behind a test-only entrypoint that rejects managed authorization.
docs-governance conforms: PB-009 updates the canonical managed-tier and Huddles execution docs to name v2 execute/status/cancel as the only managed RPC and ensure/invoke as local-only compatibility, matching the shipped entrypoint split.
managed-tier-do-gateway conforms: PB-011 binds execute/cancel retry identity to the invocation ID at decode, requires positional immutable evidence for completion, and terminalizes zero-event runtime success as a bounded runtime failure without adding persistence or collaboration authority.
managed-tier-do-gateway conforms: PB-010 admits only OpenCode before every managed snapshot, persistence, credential, provisioning, allowance, and network path; the executable Codex smoke records the real fail-closed boundary without adding a Durable Object or managed execution.
managed-store-of-record conforms: PB-010 rejects unsupported managed agents before rustic workspace resolution or snapshotting; rustic-over-R2 remains the unchanged authoritative workspace store for admitted OpenCode runs.
docs-governance conforms: PB-010 narrowly documents the first-line managed agent admission invariant and names observed executable preflight evidence as the release requirement.
managed-tier-do-gateway conforms: PB-012 records one bounded invocation across execute, exact retry, paginated status, observed preflight, and explicit finalize cleanup; it preserves one D1 allowance reservation, one immutable R2 artifact, one cost envelope, and no custom Durable Object or collaboration authority.
session-event-log-spine conforms: PB-013 couples the detached transcript process to its sole producer thread so PID health cannot remain green after the append-only session-log producer terminates; sequence and append authority are unchanged.
dx-zero-config-local conforms: PB-013 lets zero-config local readers replace a dead detached producer instead of deferring forever to a stale PID; all readers still consume the same local session log.
gateway-no-daemon conforms: PB-013 changes only the lifecycle of the re-exec'd per-session producer and adds no daemon, gateway authority, or remote placement.
substrate-plane-livesession conforms: PB-013 preserves the LiveSession replacement path and capability boundary while making the detached producer's signal-0 health reflect its nested watcher.
optimization-external-substrate-primitives conforms: PB-013 repairs the local evidence producer lifecycle without adding optimization, routing, or policy logic to Pillbox.
managed-tier-do-gateway conforms: PB-015 rejects unsupported requests before bounded D1 allowance claims, records only confirmed fail-loud Analytics writes, validates the isolated vendor-Sandbox topology and applied Huddles authority receipt, and seals request, execution, positional evidence, artifact, and cost identity without adding collaboration authority or a Pillbox-authored Durable Object.
docs-governance conforms: PB-015 updates the managed preview runbook and executable report/bootstrap contracts together, preserving the canonical single-controller boundary and default-deny topology.
managed-tier-do-gateway conforms: XR-017 makes Huddles the sole live runtime recorder for one private service-binding invocation, exact retry, and bounded status sequence; Pillbox performs only the preflight and the separately scoped public finalize after validating terminal report-v3 identity, evidence, artifact, and cost fields, without another allowance or execution claim.
docs-governance conforms: XR-017 updates the recorder, reconciler, fixture, and runbook as one checked report-v3 procedure with cleanup attached to the authoritative Huddles report only after terminal evidence.
managed-tier-do-gateway conforms: PB-018 splits the bounded execution contract, client, and transient pending journal from the backend adapter; prepared-to-committing recovery persists no bearer, adds no remote sequencer or custom Durable Object, and still commits managed evidence only to the caller's local SessionLog.
managed-store-of-record conforms: PB-018 changes only managed execution response recovery and the local evidence commit; rustic-over-R2 remains the authoritative workspace store and handle space.
session-event-log-spine conforms: PB-018 uses the existing per-session flock and authoritative log sequence to journal one exact pre-append payload batch, recover only its matching prefix, fsync the append before journal clear, and fail loud on interleaving or mismatch without adding a second event authority.
managed-tier-do-gateway conforms: PB-017 writes one deterministic immutable R2 artifact before the terminal D1 CAS and permits only that CAS winner to attempt one derivative best-effort Analytics point; retries and recovery cannot re-emit after D1 terminality, and no custom Durable Object or collaboration authority is added.
docs-governance conforms: PB-017 updates the canonical managed-tier, Durable Object usage, and burn-in docs with the shipped R2-to-D1-to-best-effort-Analytics ordering and planned-versus-provider-observed report-v3 contract.
managed-tier-do-gateway conforms: XR-020 binds the reviewed allowance epoch to deterministic execution identity, proves the live D1 allowance before and after execution, and durably claims public finalize before kill/backup/snapshot so exact retries only replay/status the canonical request; no custom Durable Object or collaboration authority is introduced.
docs-governance conforms: XR-020 updates the validator, report-v3 recorder/reconciler, fixture, and managed-preview runbook together with the applied D1 receipt, live allowance, and crash-safe finalize contracts.
managed-tier-do-gateway conforms: PB-021 rejects empty rendered input and serialized requests over the shared 1 MiB bound before creating a Prepared journal, while retaining the journal for valid requests whose post-send response is rejected or ambiguous.
session-event-log-spine conforms: PB-021 changes only pre-journal managed request admission and preserves the existing journal-backed recovery boundary for every request that may have crossed the HTTP side-effect boundary.
managed-tier-do-gateway conforms: XR-022 requires the live reviewed allowance at reserved zero before execute and reserved one after bounded status, derives and checks the canonical finalize request digest and ID for direct or status recovery, and verifies the rustic result snapshot has the requested base as an actual parent before persisting cleanup; no custom Durable Object or collaboration authority is added.
managed-store-of-record conforms: XR-022 inspects returned snapshot ancestry through the existing Pillbox rustic repository before recording the result; rustic-over-R2 remains the authoritative workspace store and handle space.
docs-governance conforms: XR-022 updates the validator-owned remote D1 query, strict report-v3 recorder/reconciler, deterministic fixture, regression tests, and managed-preview runbook as one checked evidence procedure.
managed-tier-do-gateway conforms: PB-023 serializes one local managed turn from pending-journal load through exact evidence commit and clear, and rejects empty provider/model requests before journaling without adding a remote authority or custom Durable Object.
session-event-log-spine conforms: PB-023 persists the detached Codex rollout byte position only after the authoritative exact session-log batch and recovers the append/cursor crash window through append_exact_batch without changing sequence authority.
substrate-plane-livesession conforms: PB-023 restores a failed detached Codex transcript producer behind LiveSession::spawn_log_tailer while preserving the sole-producer boundary for every reader.
optimization-external-substrate-primitives conforms: PB-023 adds only local session serialization and durable transcript evidence recovery; no routing, optimization, or policy authority enters the substrate.
managed-tier-do-gateway conforms: PB-024 replaces only a literal-only formatting call in managed workspace transfer validation; the prefix-scoped R2 credential boundary and fail-closed error semantics are unchanged.
managed-store-of-record conforms: PB-024 is a strict-Clippy gate repair in transfer validation; rustic-over-R2 remains the authoritative workspace store and handle space.
session-event-log-spine conforms: BND-001 bounds untrusted local transcript discovery and records plus durable log records, while preserving the flock-serialized per-session sequence authority, exact append-prefix crash recovery, and fail-loud immutable history.
dx-zero-config-local conforms: BND-001 keeps every reader on the same zero-config local session log and changes only bounded producer admission and append/replay implementation work.
libkrun-is-the-backend conforms: IMG-001 preserves the complete pinned Codex native package in the OCI rootfs consumed by libkrun; it adds no backend or changes backend selection.
docker-backend-deleted conforms: IMG-001 changes only runner image/build packaging, which remains the permitted Docker role; it adds no Docker agent-runtime feature.
runner-image-tags-name-roles conforms: IMG-001 leaves CODEX_VERSION pinned and changes no dev/latest/vX.Y.Z tag role or image selection.
docs-governance conforms: IMG-001 documents the stable Codex package layout and companion verification in the canonical runner-image guide, matching the Dockerfile and build gate.
libkrun-is-the-backend conforms: RFS-001 preserves OCI archive permissions in the host-owned v3 rootfs materialized for the sole local microVM backend and version-gates cached exports without changing backend selection.
docker-backend-deleted conforms: RFS-001 uses Docker only to export the runner image into a host-authorized generation whose rootfs child serves libkrun; it adds no Docker agent-runtime behavior or parity surface.
runner-image-tags-name-roles conforms: RFS-001 hashes the exact image reference below the unchanged dev/latest/vX.Y.Z tag roles, and confines explicit pruning to that current-format namespace.
docs-governance conforms: RFS-001 documents permission-preserving extraction, host-only bounded cache authority, strict v3 fallback selection, and the explicit prune boundary alongside the implementation.
managed-tier-do-gateway conforms: BND-002 reserves the shared D1 allowance and binds an opaque authorization-derived session owner before Sandbox restore or execution, keeps every lifecycle lookup owner-qualified, rejects legacy unowned rows, and adds no custom Durable Object, collaboration authority, unbounded query, or execution/2 wire identity.
managed-store-of-record conforms: BND-002 persists only transport-safe ownership and reservation digests in relational metadata; workspace bytes and lineage remain authoritative in the unchanged rustic-over-R2 handle space, and scoped transfer credentials are never stored.
session-event-log-spine conforms: BND-002 prepares and journals the exact initial managed invocation before provision, then reuses the existing per-session lock and exact append recovery for its first execution; no remote event authority or alternate sequence is introduced.
docs-governance conforms: BND-002 updates the managed lifecycle, D1 cost boundary, bootstrap migration set, and preview instructions together without changing a ratified decision.
libkrun-is-the-backend conforms: OWN-001 normalizes only disposable per-run home and workspace clones inside the selected libkrun guest before agent execution; backend selection and the default feature are unchanged.
libkrun-env-fork-substrate conforms: OWN-001 changes guest-visible ownership of the cloned credential home without moving real credentials into guest env, argv, or VmSpec, and leaves the per-VM egress and swap boundary unchanged.
workspace-ingest-sovereignty conforms: OWN-001 applies after the existing pillbox-controlled secret scrub and only to the resulting workspace clone; source workspaces, warm bases, and denylist authority remain untouched.
docs-governance conforms: OWN-001 records clone ownership, exact mode preservation, physical symlink handling, and excluded host-source/rootfs targets in the canonical libkrun sandbox guide alongside the implementation.
libkrun-is-the-backend conforms: META-001 prepares only disposable libkrun workspace and credential clones before virtio-fs mount; backend selection and non-macOS behavior are unchanged.
libkrun-env-fork-substrate conforms: META-001 changes only guest-visible override metadata on private clones, retains the existing guest env/argv/VmSpec credential boundary, and leaves egress and swap channels unchanged.
workspace-ingest-sovereignty conforms: META-001 runs after the canonical workspace secret scrub, never touches source or warm-base paths, and rejects symlink-following or unsupported clone entries.
docs-governance conforms: META-001 records libkrun's source-backed override-stat format, temporary owner-write/restore invariant, effective-mode preservation, and excluded rootfs/grader/source targets in the canonical sandbox guide.
docs-governance conforms: META-001 follow-up records the bounded temporary owner-access adjustment for unreadable/searchless private-clone entries and its exact restoration boundary.
session-event-log-spine conforms: Codex 0.151 transcript ingestion maps modern custom-tool pairs and standalone incremental Usage into the existing single-writer log, persisting the cumulative baseline with the exact cursor batch and preserving bounded replay/fail-loud recovery.
session-event-log-spine conforms: Logging tighten removes an internal snapshot wrapper and redundant branches without changing serialized parser state, validation failures, or the atomic log/cursor commit boundary.
session-event-log-spine conforms: Guard health checks observe producer termination without changing event sequencing, append locking, or rejected-record cursor semantics; overflow trips instead of corrupting monitoring arithmetic.
session-event-log-spine conforms: Current Codex host-spawn failures map to the existing ToolStatus error path and spans use their harness identity; wire schemas and durable accounting checkpoints remain unchanged.
gateway-no-daemon conforms: Producer-health polling belongs to the foreground guard, uses the existing detached ownership lock, and adds no resident coordinator or new event authority.
dx-zero-config-local conforms: Guard health and overflow checks use local thread/process/file state and the existing log subscription without requiring telemetry infrastructure.
dx-zero-config-local conforms: Codex transcript accounting remains local-only and uses the existing session log/cost projection; the human missing-usage display distinguishes unavailable accounting without changing the JSON contract.
optimization-external-substrate-primitives conforms: The Codex transcript fix records execution usage and tool evidence only; it adds no routing, optimization, or policy authority to Pillbox.
docs-governance conforms: The canonical session-event-log document records the modern Codex mapping, cumulative usage semantics, cursor recovery boundary, and human-only missingness behavior alongside the implementation.
session-event-log-spine conforms: A cache-write counter first reported after an omitted snapshot emits its cumulative value once; restored accounting state, duplicate suppression, and the existing exact log/cursor commit boundary remain unchanged.
managed-tier-do-gateway conforms: Removing a redundant test-only Read import preserves bounded managed HTTP execution, authorization, and persistence behavior.
libkrun-env-fork-substrate conforms: Bound unsent MITM request bytes and preserve partial upstream writes and cross-poll credential substitution within the existing host-owned per-VM egress boundary; no real credentials enter guest state.
libkrun-is-the-backend conforms: The bounded MITM queue changes only the existing local libkrun relay and leaves backend selection unchanged.
docs-governance conforms: The canonical vault guide records the enforced unsent-byte bound, overflow reset, accepted-prefix forwarding, and HTTP-boundary substitution behavior alongside the implementation.
managed-tier-do-gateway conforms: Receipt v2 adds only operator-side source-backed accounting scopes for snapshots, authority requests, and vendor lifecycle usage; immutable runtime envelopes, bounded persistence, single-controller ownership, and default-deny Durable Object topology remain unchanged.
docs-governance conforms: The managed-tier guide, Durable Object policy, and burn-in receipt/runbook distinguish artifact budgets from measured operator overhead and namespace retention; no production enablement or complete dollar attribution is implied.
managed-tier-do-gateway conforms: Receipt validation bounds malformed operator evidence and brackets the vendor lifecycle baseline without changing runtime authority, persistence, or immutable cost envelopes.
ghost-extraction-trigger conforms: Removing the unused test-only drive_segments wrapper changes no dispatch mechanism; the segment-chain executor stays in pillbox behind drive_segments_with and ghost still consumes only the CLI and §0 contracts.
managed-tier-do-gateway conforms: Temporary R2 credentials may share the parent access-key ID but require a nonempty fresh secret and session token; the parent secret fails closed and prefix scoping is unchanged.
managed-store-of-record conforms: Only transfer credential validation changes; rustic-over-R2 remains authoritative and no new storage or fork mechanism is introduced.
docs-governance conforms: The managed-tier guide records the temporary response shape and fresh-secret boundary alongside the recovered implementation.
session-event-log-spine conforms: The detached-producer guard test uses a synchronized child to isolate flock ownership from concurrent test forks; runtime ownership checks, event sequencing, and producer lifecycle are unchanged.
dx-zero-config-local conforms: The lock-test child uses local temporary files and pipes only; no runtime dependency or privileged event-reader path is introduced.
gateway-no-daemon conforms: The short-lived test helper models detached lock ownership and exits under its test parent; no resident runtime coordinator is added.
optimization-external-substrate-primitives conforms: The test-only synchronization changes no reward, optimization, event, or pooling contract.
substrate-plane-livesession conforms: The guard regression fixture remains backend-independent and changes neither LiveSession capabilities nor runtime command dispatch.
dx-zero-config-local conforms: `conditions[]` is derived from the existing per-session log fold and printed by the same local `session info|diagnose|list` readers; no collector, daemon, or privileged reader is introduced, and `--preset` only folds descriptor fields under the existing run flags.
substrate-plane-livesession conforms: The condition set is computed from `events::status::summarize` and the lifecycle sink, not from backend-specific dispatch; `LiveSession` capabilities and command routing are unchanged.
gateway-no-daemon conforms: Conditions are a read-only projection over the log at command time; no resident process, lock holder, or seq authority is added.
optimization-external-substrate-primitives conforms: Presets and conditions are substrate ergonomics (declared run environments, a typed status projection); no reward, selection, or optimization policy moves into pillbox.
docs-governance conforms: AGENTS.md gains one quick-start line for `--preset`; the canonical descriptor doc (docs/config.md) and command reference carry the full contract, and the stale backend note in docs/dx.md now matches `select_backend`.
runner-image-tags-name-roles conforms: Harness pins and Prime packaging change only image contents; dev remains the local build default and immutable images remain available for sealed profiles.

managed-tier-do-gateway amend-proposed: Allow explicitly selected DigitalOcean managed execution alongside default Cloudflare, preserving authorization, exact retries, bounded D1/R2 persistence, and rustic workspace authority.

managed-tier-do-gateway conforms: Ratified revision 0003 permits the explicit DO transport; bounded allocation records, exact execution hashes, owner-qualified cleanup, D1/R2 persistence, and deny-all admission preserve the execution boundary.
managed-store-of-record conforms: The DO adapter accepts sealed prompt-only invocations and rejects adoption of provisioned workspaces; it introduces no alternative workspace store or snapshot handle.
docs-governance conforms: Managed-tier and DO adapter documentation identify supported capabilities, immutable config setup, bounded cleanup, cost missingness, and unrun live release gates alongside the implementation.

managed-tier-do-gateway conforms: Review fixes bind normalized effort to explicit native OpenRouter variants, reject unsupported model/effort pairs before allocation, and preserve provider routing and exact retries.

managed-tier-do-gateway conforms: Live smoke correction admits only completed native StructuredOutputError for the existing sealed-schema fallback; arbitrary tools, unrelated errors, and invalid output remain rejected.
