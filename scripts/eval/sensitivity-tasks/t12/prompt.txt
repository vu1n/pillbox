Implement `balanced_diff(xs)` in `solution.py`: return the absolute difference between the sum of even-indexed and odd-indexed elements of `xs`.

`xs` is a list of integers. Edit ONLY `solution.py`. Keep the function name and
signature exactly as given.
