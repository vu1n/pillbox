Implement `count_runs(xs)` in `solution.py`: return the number of maximal runs of equal adjacent values in `xs`.

`xs` is a list of integers. Edit ONLY `solution.py`. Keep the function name and
signature exactly as given.
