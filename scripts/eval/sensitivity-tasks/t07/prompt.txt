Implement `abs_max(xs)` in `solution.py`: return the value in `xs` with the largest absolute value (return the value itself, keeping its sign).

`xs` is a list of integers. Edit ONLY `solution.py`. Keep the function name and
signature exactly as given.
